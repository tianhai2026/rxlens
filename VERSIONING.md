# RxLens 版本升级规则

## 版本号格式

采用语义化版本控制（Semantic Versioning），格式为：

```
vMAJOR.MINOR.PATCH
```

### 版本号组成说明

| 组成部分 | 含义 | 升级条件 |
|---------|------|---------|
| **MAJOR** | 主版本号 | 重大功能变更、API不兼容、架构重构 |
| **MINOR** | 次版本号 | 新增功能、向后兼容的改进 |
| **PATCH** | 修订号 | Bug修复、小改进、文档更新 |

---

## 升级规则

### 1. 主版本升级 (MAJOR)

**触发条件：**
- 项目架构发生重大变化
- 数据库Schema不兼容变更
- API接口破坏性变更
- 核心业务逻辑重构

**升级流程：**
1. 编写详细的升级迁移文档
2. 创建数据库迁移脚本
3. 进行完整回归测试
4. 更新版本号并发布

**示例：**
- `v1.0.0` → `v2.0.0`

---

### 2. 次版本升级 (MINOR)

**触发条件：**
- 新增功能模块
- 新增页面/组件
- 引入新的第三方依赖
- 性能优化（显著提升）
- UI/UX重大改进

**升级流程：**
1. 更新功能文档
2. 添加单元测试
3. 测试新功能
4. 更新版本号

**示例：**
- `v1.0.0` → `v1.1.0`

---

### 3. 修订版本升级 (PATCH)

**触发条件：**
- Bug修复
- 代码优化（不影响功能）
- 文档更新
- 样式微调
- 依赖版本更新

**升级流程：**
1. 确认修复内容
2. 进行回归测试
3. 更新版本号

**示例：**
- `v1.0.0` → `v1.0.1`

---

## 版本备份规则

### 备份时机

| 时机 | 备份类型 | 说明 |
|------|---------|------|
| 每次版本升级前 | 完整备份 | 确保可回滚 |
| 重大功能开发前 | 完整备份 | 防止开发风险 |
| 每周五下班前 | 增量备份 | 定期备份 |

### 备份目录结构

```
rxlens-backups/                # 统一备份文件夹
├── v1.0.0/                    # 版本号目录
│   ├── src/                   # 源代码
│   ├── public/                # 静态资源
│   ├── package.json           # 依赖配置
│   ├── next.config.js         # 配置文件
│   ├── backup-info.md         # 备份信息
│   └── timestamp.txt          # 备份时间戳
├── v1.0.1/                    # 后续版本备份
└── v1.1.0/
```

### 备份信息文件格式 (backup-info.md)

```markdown
# 备份信息

## 版本号
v1.0.0

## 备份时间
2024-01-15 14:30:00

## 备份原因
第一阶段任务完成，进入第二阶段前备份

## 主要功能
- [x] 暗色系科技感UI
- [x] 本地数据库建表
- [x] 处方/小票手动录入表单
- [x] 毛玻璃预览表单组件
- [x] 金额校验日志记录

## 已知问题
- 暂无

## 备注
备份后可安全进行第二阶段开发
```

---

## 版本号管理

### 当前版本记录

| 版本 | 状态 | 日期 | 说明 |
|------|------|------|------|
| v1.0.0 | ✅ 已发布 | 2024-01-15 | 第一阶段完成：基础UI+数据库+表单 |

### 版本文件位置

版本号统一记录在以下位置：
1. `package.json` → `version` 字段
2. 页面底部显示版本号
3. 备份目录名称

---

## 升级检查清单

### 升级前检查
- [ ] 确认当前代码已提交
- [ ] 运行所有测试用例
- [ ] 创建当前版本备份
- [ ] 检查依赖版本兼容性

### 升级后检查
- [ ] 更新版本号
- [ ] 更新CHANGELOG
- [ ] 运行回归测试
- [ ] 验证数据库连接
- [ ] 测试核心功能

---

## 变更日志 (CHANGELOG)

### v1.0.0 (2024-01-15)
- 初始化 Next.js 14 项目
- 配置深色主题（背景色 #121212，强调色 #00F0FF）
- 集成 TailwindCSS 和 shadcn/ui
- 创建本地数据库 (Dexie.js)
- 实现处方/小票手动录入表单
- 实现毛玻璃预览表单组件
- 添加金额校验日志记录功能
- 创建测试页面和日志查看器

---

## 回滚流程

如升级后发现严重问题，按以下步骤回滚：

1. **停止服务**
   ```bash
   Ctrl+C (开发环境)
   ```

2. **恢复备份**
   ```bash
   # 删除当前版本文件
   rm -rf src/ public/ package.json
   
   # 从备份恢复
   cp -r backups/v1.0.0/* .
   ```

3. **重新安装依赖**
   ```bash
   npm install
   ```

4. **验证恢复**
   ```bash
   npm run build
   npm run dev
   ```

---

## 版本发布流程

1. **代码审查** - 确保代码质量
2. **测试验证** - 运行完整测试套件
3. **更新版本号** - 修改 package.json
4. **创建备份** - 执行备份脚本
5. **更新文档** - 更新 CHANGELOG 和 README
6. **发布通知** - 告知相关人员

---

## 注意事项

1. **版本号唯一性**：每个版本号只能使用一次
2. **备份完整性**：确保备份包含所有源代码和配置文件
3. **测试覆盖**：升级前必须通过所有测试
4. **文档同步**：版本升级时同步更新相关文档
5. **沟通及时**：重大升级需提前通知团队成员